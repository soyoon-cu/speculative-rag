# Copy this file to config.mk and fill in your values.
# config.mk is gitignored — never commit real credentials.

PROJECT_ID = your-gcp-project-id
REGION     = us-central1
REPO_NAME  = hpml-repo
IMAGE_NAME = speculative-rag

INDEX_BUCKET  = standard-rag-results-2026
OUTPUT_BUCKET = speculative-rag-results-2026
